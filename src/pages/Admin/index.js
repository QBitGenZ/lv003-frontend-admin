import { Header, SideBar } from "../../common";

const Admin = () => {
    return (
        <div id='Admin'>
            <Header currentPage={"Trang chủ"} />
            <SideBar />
        </div>
    );
};

export default Admin;
